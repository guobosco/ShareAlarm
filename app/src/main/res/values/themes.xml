<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.ShareAlarm" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Window attributes -->
        <item name="android:windowBackground">@color/white</item>
        <item name="android:fontFamily">sans-serif</item>
        
        <!-- Basic color attributes -->
        <item name="android:colorPrimary">@color/nasa_blue</item>
        <item name="android:colorPrimaryDark">@color/nasa_blue_dark</item>
        <item name="android:colorAccent">@color/nasa_blue_light</item>
        <item name="android:colorBackground">@color/white</item>
    </style>
</resources>
